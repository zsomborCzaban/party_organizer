meta {
  name: Post
  type: http
  seq: 1
}

post {
  url: {{BACKEND_URL}}{{API_PATH}}/foodContribution
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{BEARER_TOKEN_JWT}}
}

body:json {
  {
    "requirement_id": 3,
    "quantity": 6,
    "description": "reszletes leiras"
  }
}
