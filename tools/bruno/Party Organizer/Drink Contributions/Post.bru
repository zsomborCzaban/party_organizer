meta {
  name: Post
  type: http
  seq: 1
}

post {
  url: {{BACKEND_URL}}{{API_PATH}}/drinkContribution
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{BEARER_TOKEN_JWT}}
}

body:json {
  {
    "requirement_id": 7,
    "quantity": 2,
    "description": "meggyes"
  }
}
