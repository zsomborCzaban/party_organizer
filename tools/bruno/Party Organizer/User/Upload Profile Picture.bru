meta {
  name: Upload Profile Picture
  type: http
  seq: 4
}

get {
  url: {{BACKEND_URL}}{{API_PATH}}/user/getFriends
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{BEARER_TOKEN_JWT}}
}
