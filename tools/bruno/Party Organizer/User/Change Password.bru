meta {
  name: Change Password
  type: http
  seq: 4
}

post {
  url: {{BACKEND_URL}}{{API_PATH}}/user/resetPassword
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{BEARER_TOKEN_JWT}}
}

body:json {
  {
      "password": "asd",
      "confirm_password": "asd"
  }
}
