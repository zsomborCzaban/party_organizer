meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{BACKEND_URL}}{{API_PATH}}/login
  body: json
  auth: none
}

body:json {
  {
      "username": "zsolt",
      "password": "asd0"
  }
}

script:post-response {
  let data = res.body;
  bru.setEnvVar("BEARER_TOKEN_JWT", data.data.jwt);
}
